#include<stdio.h>
#include "stack.h"
int main()
{
int N,num;
scanf("%d",&N);
while(N--){
    scanf("%d",&num);
    if(num>9){
        push(num);
    }
    else{
        pop();
    }
}
    while(getSize()>=0)
    {
        printf("%d ",pop());
    }
}
